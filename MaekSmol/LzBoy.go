package MaekSmol

// This should do the LZ77 algorithm analysis stuff and
func LZ77() {
    // start window
    // pull in first character
    // loop through, checking if the 
}

// This should be the helper function to find if something is in 
// the sliding window
func CheckWindehr() {
}
